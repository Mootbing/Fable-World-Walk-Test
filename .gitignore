/node_modules
/.next/
/out/
/build

# Rust build outputs (sim/pkg + public/sim_bg.wasm are COMMITTED so
# deploys build with plain next build — no Rust toolchain needed there;
# regenerate with npm run sim:build before release commits)
/sim/target/
# machine-specific cargo config (linker workaround)
/.cargo/

# playwright
/test-results/
/playwright-report/
.DS_Store
*.pem
npm-debug.log*
.env*.local
*.tsbuildinfo
next-env.d.ts
